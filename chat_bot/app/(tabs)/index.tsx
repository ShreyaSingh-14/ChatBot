import {View} from 'react-native'
// Update the import path and extension as needed, for example:
import HomeScreen from './../../App_new/Pages/HomeScreen'
// or, if the file is named HomeScreen.tsx or index.tsx inside pages, adjust accordingly
// import HomeScreen from './../../App_new/pages';

export default function App() {
  return (
    <View>
      <HomeScreen/>
    </View>
  );
}